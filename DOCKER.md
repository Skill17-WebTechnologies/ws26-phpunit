# PHPUnit 12.5.12 — WSC2026 test project

```bash
docker compose up --build
```

`docker compose up` runs the PHPUnit test suite (a small `Calculator` class + tests) and prints
the green result, then exits. Pinned: PHP 8.3 / Composer 2.9.5, phpunit/phpunit 12.5.12.
